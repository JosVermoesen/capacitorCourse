import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.vsoft.capacourse',
  appName: 'capacitorCourse',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
